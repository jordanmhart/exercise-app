extends ../layout 

block content
    p.lead Registration
        br
        form(role='form', action="/register", method="post", style='max-width: 500px;')
            .form-group
                input.form-control(type='text', name="email", placeholder='Enter Email')
            .form-group
                input.form-control(type='text', name="full_name", placeholder='Enter Full Name')
            .form-group
                input.form-control(type='text', name="initials", placeholder='Enter Initials')
            .form-group
                input.form-control(type='password', name="password", placeholder='Enter Password')
            .form-group
                input.form-control(type='password', name="password-confirmation", placeholder='Re-enter Password')
            button.btn.btn-primary(type='submit') Submit
            span  
            a(href='/')
                button.btn.btn-default(type="button") Cancel
            p Not hustlin' for the holidays?  
            a(href='/register') Register
            span  
            span here.